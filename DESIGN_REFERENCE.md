# ShopAgent UI Design Reference

The production web UI uses the approved premium dark command-center direction: persistent sidebar, AI command hero, To-Buy and Monitoring as separate workflows, live activity, deal intelligence, store comparison, orders, savings, and safety controls.

The generated visual mockup supplied during this build is the visual target. The implementation uses live API state rather than hard-coded mock metrics.
